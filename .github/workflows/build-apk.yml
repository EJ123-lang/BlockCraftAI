name: Build Android APK

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - "App.js"
      - "src/**"
      - "package.json"
      - "package-lock.json"
      - "app.json"
      - ".github/workflows/build-apk.yml"

jobs:
  build-apk:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      contents: read

    steps:
      - name: Checkout source
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - name: Install dependencies
        run: npm install --legacy-peer-deps

      - name: Prepare app icons
        run: |
          mkdir -p assets
          base64 --decode assets/icon.png.b64 > assets/icon.png
          cp assets/icon.png assets/adaptive-icon.png

      - name: Generate native Android project
        run: npx expo prebuild --platform android --non-interactive --clean

      - name: Build standalone release APK
        working-directory: android
        env:
          NODE_ENV: production
        run: ./gradlew assembleRelease --no-daemon --stacktrace

      - name: Rename APK
        run: cp android/app/build/outputs/apk/release/app-release.apk BlockCraftAI-v1.0.2.apk

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: BlockCraftAI-Android-APK
          path: BlockCraftAI-v1.0.2.apk
          if-no-files-found: error
          retention-days: 30
          compression-level: 0
